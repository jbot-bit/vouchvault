.codex
.config/
.local/
.git.broken*/
.git.placeholder
.mastra/.build/
.mastra/output/
attached_assets/
node_modules/

# Legacy export JSON files (replay input + checkpoints/reports)
Export/
imports/
*.legacy-import-checkpoint.json
*.legacy-import-review.json
# Sensitive third-party group export (e.g. result_tbc.json)
result_tbc.json

# Local environment files
.env
.env.*
# ...but DO track the template.
!.env.example

# Per-machine Claude Code permission state (the shared settings.json stays tracked)
.claude/settings.local.json

# OS/editor noise
Thumbs.db
.DS_Store

# Telethon userbot session + forward-progress state (scripts/userbot_forward.py)
scripts/userbot.session
scripts/userbot.session-journal
scripts/userbot_forward_state.json
railway-env.txt
